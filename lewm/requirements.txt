# LeWorldModel dependencies
# Venv path is set as WMA_VENV in env.sh (see env.sh.example).

# PyTorch — install with CUDA index first:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124

# Core research libraries (install from GitHub):
# pip install "stable-worldmodel[train] @ git+https://github.com/galilai-group/stable-worldmodel.git"
# (this pulls in stable-pretraining as a dependency)

# Additional
einops
lightning
imageio[ffmpeg]
